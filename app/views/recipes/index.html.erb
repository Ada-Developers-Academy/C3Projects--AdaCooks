<h1>
  All Recipes
  <% if session[:user_id] %>
    <small>+<%= link_to "Add New Recipe", new_user_recipe_path(@authenticated_user.id) %></small>
  <% end %>
</h1>

<div class="col-sm-8 col-sm-offset-2">
  <%= render partial: 'shared/recipes_list', locals: { recipes: @recipes } %>
</div>
