

<%= link_to "#{@user.name}",user_path(@user.id) %>  


<% @recipe.ingredients.each do |ingredient| %>
  <ul>
    <li> <%= image_tag(ingredient.image) %> </li>
    <li> <%= link_to "#{ingredient.name}", ingredient_path(ingredient.id) %> </li>
  </ul> 
<% end %> 
