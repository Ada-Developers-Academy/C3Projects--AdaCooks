<% if session[:user_id] %>
  <div class="text-right">
    <%= link_to edit_cookbook_path(@cookbook.id), class: 'btn btn-primary' do %>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    <% end %>
    <%= link_to cookbook_path(@cookbook.id), method: :delete, class: 'btn btn-danger' do %>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    <% end %>
  </div>
<% end %>

<%= render partial: "shared/recipes" %>
