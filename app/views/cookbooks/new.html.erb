<%= form_for @cookbook do |f| %>
  <fieldset>
    <legend>Cookbook Details</legend>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_field :description %>
  </fieldset>

  <%= f.submit %>
<% end %>