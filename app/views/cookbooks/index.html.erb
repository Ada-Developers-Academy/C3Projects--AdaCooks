<h3 class="text-center"><i class="fa fa-book"></i> Cookbooks</h3>
<% if session[:user_id]%>
  <div class="text-center">
    <%= link_to "Add a New Cookbook", new_user_cookbook_path(session[:user_id]), class: 'btn btn-primary' %>
  </div>
<% end %>

<%= render partial: "shared/cookbooks" %>

<a href="#top">Back to Top</a>
