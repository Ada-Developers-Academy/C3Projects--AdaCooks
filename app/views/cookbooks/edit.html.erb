<%= render partial: "form", locals: { form_type: "Edit",
                                      path: user_cookbook_path(@cookbook.user_id, @cookbook.id),
                                      method: :patch } %>
