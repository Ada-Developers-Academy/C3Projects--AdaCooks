<%= form_for @cookbook, url: user_cookbook_path(session[:user_id], @cookbook.id) do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit %>
  </fieldset>
<% end %>
