<ul class="nav navbar-nav navbar-left">
  <li>
    <%= form_for :search, url: root_path, :html => { :class => "navbar-form pull-right" } do |f| %>
      <div class="form-group">
        <%= f.text_field :query, class: "form-control", placeholder: "Ingredient Name" %>
      </div>

      <%= f.submit "Search", class: "btn btn-default" %>
    <% end %>
  </li>
</ul>

<% if @authenticated_user %>
<ul class="nav navbar-nav navbar-right">
  <!-- user contributions dropdown -->
  <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
        Create New.. <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to "Cookbook", new_user_cookbook_path(@authenticated_user) %></li>
        <li><%= link_to "Recipe", new_user_recipe_path(@authenticated_user) %></li>
        <li><%= link_to "Ingredient", new_user_ingredient_path(@authenticated_user) %></li>
      </ul>
  </li>
  <li><%= link_to "View Profile", user_path(@authenticated_user) %></li>
  <li><%= link_to "Logout", logout_path, method: "delete", class: "pull-right" %></li>
</ul>
<% else %>
<ul class="nav navbar-nav navbar-right">
  <li><%= link_to "Signup", signup_path, class: ".navbar-right" %></li>
  <li><%= link_to "Login", login_path, class: ".navbar-right" %></li>
</ul>

<% end %>
