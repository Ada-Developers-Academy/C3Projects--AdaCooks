<h1>
  All Ingredients
  <% if session[:user_id] %>
    <small>+<%= link_to "Add New Ingredient", new_user_ingredient_path(@user) %></small>
  <% end %>
</h1>

<div class="columns">
  <%= render partial: 'shared/ingredients_list', locals: { ingredients: @ingredients } %>
</div>
