<h1>All Ingredients</h1>

<div class="row">
  <div class="col-xs-8 col-md-6">
    <ul class="list-group">
    <% @ingredients.each do |ingredient| %>
        <li class="list-group-item"><%= link_to ingredient.name, ingredient_path(ingredient) %></li>
    <% end %>
  </div>
</div>
