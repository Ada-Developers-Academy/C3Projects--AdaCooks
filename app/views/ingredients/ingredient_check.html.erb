<div class="col-md-6 col-md-offset-3">
  <h4>Do you have all the ingredients for your recipe in the pantry?</h4>

  <% @ingredients.each do |ingredient| %>
    <div class="col-md-3">
      <div class="well center">
      <%= image_tag(ingredient.image_url(:thumb), class: "img-circle", size: "50x50") %><br>
      <span class="text-small"><%= ingredient.name %></span>
      </div>
    </div>
  <% end %>

</div>
  
<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <br>
    <%= link_to "Continue to Add Recipe", new_recipe_path, class: "btn btn-primary" %>
    <%= link_to "Add More Ingredients", new_ingredient_path, class: "btn btn-default" %>
  </div>
</div>
